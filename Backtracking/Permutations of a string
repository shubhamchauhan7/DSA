void solve(string &s, int i)
{
  if(i >= s.length())
  {
    cout << s << " ";
    return;
  }

  for(int j = i; j < s.length(); j++)
  {
    swap(s[i], s[j]);
    solve(s, i + 1);
    swap(s[i], s[j]);
  }
  return;
}

int main()
{
  int i = 0;
	string s = "abc";
	solve(s, i);
	
}
